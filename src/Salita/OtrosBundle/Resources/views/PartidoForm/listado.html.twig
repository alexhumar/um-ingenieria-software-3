{% extends '::base.html.twig' %}

{% block contenido %}
	{% for partido in partidos %}
  		{{partido.nombre}}<a href="{{ path('modif_partido', { 'id': partido.id }) }}">
      		Editar
  		</a>
	{% endfor %}
{% endblock %}