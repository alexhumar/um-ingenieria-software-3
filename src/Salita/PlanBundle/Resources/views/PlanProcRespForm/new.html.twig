{% extends "SalitaPlanBundle::base.html.twig" %}

{% block stylesheets %}
  {{ parent() }}
  <link href="{{ asset('css/errores.css') }}" rel="stylesheet"/>
{% endblock %}

{% block contenido %}
    <h3>
        {% trans %}
            planbundle.PlanProcRespForm.new.titulo
        {% endtrans %}
    </h3>
    <br/>
    {% set periodicidadPlaceholder = 'planbundle.PlanProcRespForm.new.periodicidadPlaceholder' | trans %}
    
    {{ form_start(form) }}
      {{ form_errors(form) }}
      <div class="form-group col-sm-12">
        {{ form_row(form.periodicidad, { 'attr': { 'placeholder': periodicidadPlaceholder } }) }}
      </div>
      <div class="form-group col-sm-12">
        {{ form_row(form.metodoAnticonceptivo) }}
      </div>
      <div class="form-group col-sm-7">
        <div class="col-sm-offset-6">
          {{ form_row(form.guardar) }}
          {{ form_row(form.guardarynuevo) }}
        </div>
      </div>
    {{ form_end(form) }}

	{% for flashMessage in app.session.flashbag.get('mensaje') %}
		<div class="alert alert-success" role="alert">
			{{ flashMessage }}
		</div>
	{% endfor %}
{% endblock %}